# 消息流服务（MessageStreamService）

> 模块位置：`knowledge-common/src/knowledge_common/message_stream/`

## 1. 设计理念

Kafka 风格的 Python 消息流门面。把"协议层 XADD/XREADGROUP/XACK"的裸操作，变成"业务层 `@consumer` 声明 + `produce` 推送"的门面抽象。

**核心承诺**：`.env` 改 `MESSAGE_STREAM_BACKEND=redis|kafka` 即可切换后端，业务代码零行修改。

---

## 2. 分层架构

```mermaid
graph TB
    subgraph 门面层
        Service["MessageStreamService<br/>全 classmethod 入口"]
        ConsumerDeco["@consumer 装饰器<br/>声明消费者"]
        Msg["Message 数据类<br/>Kafka 风格字段"]
    end

    subgraph 抽象层
        Backend["StreamBackend<br/>6 个方法接口"]
        Factory["create_backend<br/>按配置创建后端"]
    end

    subgraph 实现层
        Redis["RedisStreamBackend<br/>XADD/XREADGROUP/XACK"]
        Kafka["KafkaStreamBackend<br/>confluent-kafka"]
    end

    Service --> Backend
    Factory --> Backend
    Backend --> Redis
    Backend --> Kafka
    ConsumerDeco --> Service
```

| 层级 | 文件 | 职责 |
|------|------|------|
| 门面层 | `service.py` | 生命周期管理 + `produce` 推送 |
| 门面层 | `consumer.py` | `@consumer` 装饰器 + `ConsumerInfo` 注册 |
| 门面层 | `message.py` | `Message` 数据类（Kafka 字段对齐） |
| 抽象层 | `backends/base.py` | `StreamBackend` 接口定义 |
| 抽象层 | `backends/factory.py` | 按 `.env` 配置动态创建后端 |
| 实现层 | `backends/redis_stream.py` | Redis Stream 实现 |
| 实现层 | `backends/kafka_stream.py` | Kafka 实现 |

---

## 3. 三步接入范式

```mermaid
sequenceDiagram
    participant LS as lifespan
    participant SVC as MessageStreamService
    participant Backend as StreamBackend
    participant Scan as 扫描路径

    LS->>SVC: 1. init_from_settings(MessageStreamConfig, redis=...)
    SVC->>Backend: create_backend() 按配置创建
    LS->>SVC: 2. register_consumer_paths(['app.message.consumer'])
    LS->>SVC: 3. await discover_and_start()
    SVC->>Scan: import 扫描路径下所有模块
    Scan-->>SVC: 触发 @consumer 装饰器注册
    SVC->>Backend: create_group(topic, group_id) 每个消费者
    SVC->>Backend: 拉起后台消费协程 _consume_loop
    SVC->>Backend: 拉起 idle 接管协程 _claim_idle_loop
```

**接入代码**（以 admin 为例）：

```python
# server.py lifespan 中
MessageStreamService.init_from_settings(MessageStreamConfig, redis=app.state.redis)
MessageStreamService.register_consumer_paths(['knowledge_admin.message.consumer'])
await MessageStreamService.discover_and_start()
# 启动自检
await AdminMessageTestPublisher.send_demo()
```

---

## 4. @consumer 注册机制

```mermaid
graph TD
    Import["Python import 模块"] --> Decorator["@consumer(topic, group_id) 执行"]
    Decorator --> GenID["生成 consumer_id = module.func_name"]
    GenID --> Check{"consumer_id 已注册?"}
    Check -->|是| Skip["跳过,不重复注册"]
    Check -->|否| Register["创建 ConsumerInfo<br/>写入 _consumers 字典"]
```

**ConsumerInfo 字段**：

| 字段 | 说明 |
|------|------|
| `consumer_id` | 全局唯一标识（`{module}.{func_name}`） |
| `topic` | 订阅主题（Kafka 风格） |
| `group_id` | 消费组 |
| `handler` | 业务处理函数 `async def(msg: Message) -> None` |
| `max_retries` | 消费侧最大重试次数（默认 3） |

---

## 5. produce 推送流程

```mermaid
graph TD
    Call["await produce(topic, value, key, headers)"] --> Check{"backend 已初始化?"}
    Check -->|否| Error["抛出 MessageStreamError"]
    Check -->|是| Retry["重试循环 (max_retries=3)"]
    Retry --> Publish["backend.publish(topic, value, key, headers)"]
    Publish -->|成功| Return["返回 msg_id"]
    Publish -->|失败| Wait["等待 retry_interval (0.5s)"]
    Wait --> Retry
    Retry -->|重试用完| Raise["抛出 MessageStreamError"]
```

---

## 6. 消费协程循环

```mermaid
graph TD
    Start["_consume_loop 启动"] --> Consume["backend.consume<br/>阻塞拉取 (block_ms=2000)"]
    Consume -->|无消息| Consume
    Consume -->|有消息| Handler["调用 handler(msg)"]
    Handler -->|成功| Ack["backend.ack(topic, group_id, offset)"]
    Handler -->|异常| LogErr["记录错误,不 ack<br/>后端 PEL 兜底"]
    Ack --> Consume
    LogErr --> Break["跳出本批"]
    Break --> Consume
```

**双协程设计**：

| 协程 | 职责 | 命名 |
|------|------|------|
| `_consume_loop` | 主消费循环，阻塞拉取 + 处理 + ack | `msg-stream:consume:{id}` |
| `_claim_idle_loop` | PEL 兜底，接管空闲超时（60s）的卡住消息 | `msg-stream:claim:{id}` |

---

## 7. Message 数据结构

字段命名对齐 Kafka，保证切 Kafka 时业务代码零修改：

| 字段 | 类型 | 说明 |
|------|------|------|
| `topic` | `str` | 主题 |
| `key` | `str \| None` | 业务键（partition key / 过滤键） |
| `value` | `Any` | 载荷 |
| `headers` | `dict` | 头部元数据 |
| `timestamp` | `int` | 毫秒时间戳 |
| `offset` | `str` | 消息位置（Stream "1234-0" / Kafka offset） |
| `partition` | `int \| None` | 分区号（Stream 无分区，值为 None） |

**兼容别名**：`msg.stream` ≡ `msg.topic`，`msg.payload` ≡ `msg.value`

---

## 8. 后端切换

`.env` 配置：

```bash
# Redis Stream 后端（默认）
MESSAGE_STREAM_BACKEND=redis
MESSAGE_STREAM_REDIS_MAXLEN=100000

# Kafka 后端
MESSAGE_STREAM_BACKEND=kafka
KAFKA_BOOTSTRAP_SERVERS=localhost:9092
```

**切换影响**：

| 层面 | 是否修改 |
|------|---------|
| `@consumer` 装饰器 | 不修改 |
| `MessageStreamService.produce` | 不修改 |
| `Message` 数据结构 | 不修改 |
| `backends/factory.py` | 自动选择 |

---

## 9. 默认扫描路径

`MessageStreamService._scan_paths` 默认包含 `knowledge_common.message.consumer`，确保 common 中集中维护的消费者（如日志聚合 `log_consumer.py`）在所有项目中自动注册。

业务项目通过 `register_consumer_paths` 追加各自专属路径（如 `knowledge_admin.message.consumer`）。
---

## 10. Redis Stream 生产-消费全流程

```mermaid
flowchart TB
    subgraph 启动阶段["① 应用启动 (lifespan)"]
        A1["初始化消息流服务<br/>注入后端实现（Redis/Kafka）"] --> A2["注册扫描路径<br/>告诉框架去哪里找消费者"]
        A2 --> A3["发现并启动<br/>扫描路径、注册消费者、拉起协程"]

        subgraph A3_1["扫描注册"]
            A3a["导入包路径下<br/>所有模块文件"] --> A3b["装饰器自动注册<br/>将消费者信息写入注册表"]
        end

        subgraph A3_2["拉起双协程"]
            A3c["遍历已注册的<br/>每个消费者"] --> A3d["创建消费组<br/>确保 Redis Stream 组已存在"]
            A3d --> A3e["消费循环协程<br/>持续拉取消息并处理"]
            A3d --> A3f["空闲接管协程<br/>处理卡住未确认的消息"]
        end

        A3 --> A3_1 --> A3_2
    end

    启动阶段 --> 生产阶段

    subgraph 生产阶段["② 消息生产 (produce)"]
        B1["业务方发送消息<br/>指定：主题、数据、业务键、头部"] --> B2["后端发布接口<br/>序列化：载荷→JSON<br/>业务键→字符串<br/>头部信息→JSON"]
        B2 --> B3["追加到 Redis Stream<br/>限制长度 10 万条<br/>防止队列无限膨胀"]
        B3 --> B4["返回消息 ID<br/>用于后续确认消费"]
    end

    生产阶段 --> 消费阶段

    subgraph 消费阶段["③ 消息消费 (_consume_loop)"]
        C1["无限循环<br/>持续消费"] --> C2["从消费组拉取消息<br/>阻塞等待 2 秒<br/>每次最多 100 条"]
        C2 --> C3{"有消息?"}
        C3 -->|无| C2
        C3 -->|有| C4["逐条执行业务<br/>处理函数 handler(msg)"]
        C4 --> C5{"处理成功?"}
        C5 -->|是| C6["确认消费<br/>告诉 Redis 已处理完成<br/>消息从待处理列表移除"]
        C5 -->|否| C7["不确认，跳出本轮<br/>记录错误日志<br/>消息留在待处理列表"]
        C6 --> C2
        C7 --> C2
    end

    消费阶段 -.->|消费异常<br/>消息滞留待处理列表| PEL兜底

    subgraph PEL兜底["④ PEL 空闲接管 (_claim_idle_loop)"]
        D1["每 5 秒扫描一次"] --> D2["接管超时未确认的消息<br/>空闲超过 60 秒的<br/>自动转移给本协程处理"]
        D2 --> D3["取回滞留的消息"]
        D3 --> D4["重新执行业务处理<br/>handler(msg)"]
        D4 --> D5["确认消费<br/>消息从待处理列表移除"]
        D5 --> D1
    end
```

### Redis Stream 协议映射

| 抽象方法 | Redis 命令 | 关键参数 / 说明 |
|---|---|---|
| `publish` | `XADD topic MAXLEN ~ 100000 __value __key __headers` | 近似裁剪，防止 Stream 无限膨胀 |
| `consume` | `XREADGROUP GROUP group consumer BLOCK 2000 COUNT 100 >` | `>` 仅消费未投递消息 |
| `ack` | `XACK topic group offset` | 批量确认，`*msg_offsets` 变参 |
| `create_group` | `XGROUP CREATE topic $ MKSTREAM` | `$` 从最新起，`MKSTREAM` 自动创建 |
| `claim_idle` | `XAUTOCLAIM topic group consumer 60000 0-0` | 接管空闲 >60s 未 ACK 的消息 |

---

## 11. 相关文档

- [广播服务](./广播服务.md)
- [日志聚合](./日志聚合.md)
- [启动流程](../architecture/启动流程与生命周期.md)
